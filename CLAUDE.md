# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際のガイダンスを提供する。

## このプロジェクトについて

**株式会社Welcart の採用選考における実技課題**の成果物リポジトリ。

Welcart（WordPress 用 EC プラグイン、開発元: 株式会社Welcart。旧コルネ株式会社、
2023-11-06 に社名変更）に対して「カート合計金額に応じた自動割引機能」を、
**独立した WordPress プラグイン**として実装する。

- 課題提示日: 2026-08-18
- 提出期限: **2026-08-28**（提示から10日以内）
- 想定作業時間: 5〜7時間程度
- 出題者: 株式会社Welcart 様

### 成果物の権利

提出物の権利は応募者に帰属する。先方が業務転用することはない、と明記されている。

## 評価の前提（実装方針に直結する重要事項）

出題文に以下が明記されている。実装判断はこれに従うこと。

- **完成度の高さよりも「判断の過程」を重視して評価される**
- 時間内に終わらなかった部分は、「なぜそうなったか」「どう実装する予定だったか」を
  書けばそれも評価対象になる。**未完了を隠して取り繕うより、正直に記述する方が高評価**
- **AIエージェント（Claude Code 等）の利用は歓迎されており、「どう使われたか」も評価対象**。
  使用を控える必要は一切ない。むしろ活用の過程を記録に残すことが求められている

→ したがって、行き詰まった箇所・AI の誤りとその修正過程は**作業中に随時記録しておく**こと。
後から思い出して書くのではなく、発生時点でメモを残す。

## 課題要件

### 必須要件

1. **独立したプラグインとして実装する**（Welcart 本体・テーマのファイル改変は不可）
2. **管理画面から、しきい値金額と割引額を複数段設定できること**
   - 例: 10,000円以上で500円引き ／ 30,000円以上で2,000円引き
3. **割引が「カート画面」「購入確認画面」「確定後の受注データ」の3箇所で整合すること**
4. **Welcart の既存フック／フィルタのみで実現すること**
5. **設定保存時の nonce 検証・入力サニタイズ・権限チェックを実装すること**
6. **WordPress コーディングスタンダード（WPCS）に準拠すること**

### 加点要件（任意）

- 特定の会員ランクや商品カテゴリを除外する条件設定
- i18n 対応
- テストコード

## 提出物（5点）

1. **プラグイン一式** — GitHub の公開リポジトリを推奨（zip でも可）
2. **README** — 動作確認手順、検証した WordPress／Welcart／PHP の各バージョン
3. **設計メモ** — 使用したフック・フィルタの一覧と選定理由。
   検討したが採用しなかった候補があれば、その比較も
4. **AI活用レポート** — 使用ツールと進め方／AI の出力が誤っていた箇所とその発見・修正方法／
   うまくいかなかったこと
5. **動作確認の記録** — スクリーンショットまたは短い画面録画

## 検証環境

WordPress および Welcart は wordpress.org から無償で取得する。
**検証環境は自前で用意する必要がある**（先方からの提供はない）。

README には実際に検証した WordPress／Welcart／PHP のバージョンを記載すること。
「動くはず」ではなく「このバージョンで動作を確認した」と書けるようにする。

## 開発上の注意

### Welcart 固有

- 接頭辞 `usces_` は Welcart 本体のもの。本プラグインの関数・フック・オプション名には
  独自の接頭辞を使い、本体と衝突させないこと
- 「カート・確認・受注データの3箇所で整合」が要件の核心。カート画面だけ割引が出て
  受注データに反映されない、といった不整合は**課題の主眼を外す**ため最優先で検証する
- フックの実在は必ず Welcart のソースを読んで確認する。**存在しないフック名を推測で
  使わない**（AI が生成しがちな誤りであり、それ自体が AI活用レポートの題材にもなる）

### セキュリティ

管理画面の設定保存処理では以下を必ず実装する（必須要件5）。

- `wp_verify_nonce()` / `check_admin_referer()` による nonce 検証
- `current_user_can()` による権限チェック
- 入力値のサニタイズ（金額は整数として扱う）と、出力時のエスケープ

### コーディング規約

WPCS 準拠が必須要件。PHP_CodeSniffer + WordPress-Coding-Standards を導入し、
**チェックを通した状態で提出する**こと。

## git 運用

- このリポジトリはプラグイン単体で独立した git リポジトリとして管理する
- 提出時に GitHub の公開リポジトリとして push する想定
- **コミット履歴も「判断の過程」を示す資料になる**。まとめて1コミットにせず、
  作業の区切りごとに意図の分かるメッセージで積むこと

## 不明点

課題文に「ご不明な点がございましたら、お気軽にご質問ください」とある。
判断に迷い、かつ推測で進めると手戻りが大きい論点は、先方に質問することも選択肢に入れる。
